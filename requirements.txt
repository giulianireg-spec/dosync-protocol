fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
aiohttp>=3.8.0
pywizlight>=0.5.0
twilio>=8.0.0
websockets>=11.0
paho-mqtt>=2.0.0

# Optional — BLE adapter (universal Bluetooth Low Energy). Only needed if
# DOSYNC_BLE_ENABLED=true. Uses BlueZ on Linux/Raspberry Pi.
# bleak>=0.21

# Schema validation — params_schema is JSON Schema draft 2020-12 (protocol 0.3).
# Optional but recommended: without it, schema validation degrades gracefully
# (skipped with a warning). The certification suite requires it.
jsonschema>=4.18
