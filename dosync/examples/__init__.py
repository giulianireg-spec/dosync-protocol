"""Worked examples shipped with the package.

They live inside `dosync/` rather than only at the repository root because
they are the deliverable, not the appendix: the format is learned by finding
an example that resembles your device and changing the address. A user who
installed from PyPI and never cloned the repository had no examples at all.
"""
